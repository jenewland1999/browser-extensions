{
  "name": "@browser-extensions/reading-list-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Import, export, and clear Chrome Reading List entries.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "lint": "oxlint src scripts test",
    "test": "pnpm run build && pnpm run test:unit",
    "test:unit": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "0.2.8",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24"
  }
}
